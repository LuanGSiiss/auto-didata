const [a, b, c, d] = [3, 5, 1, 15]

//soma
const soma = a + b + c + d // 24
const subtracao = d - b // 10
const multiplicacao = a * b // 15
const divisao = d / a  // 5
const modulo = a % 2 // 1

console.log(soma, subtracao, multiplicacao, divisao, modulo)

//Existem também os unarios, que são com apenas um valor
// -a , isso vai fazer a ser negativo


//existem os operados pos-fixed e pre-fixed
//  d++ pos-fixed
//  ++d pre-fixed

